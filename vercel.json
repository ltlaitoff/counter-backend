{
	"version": 2,
	"env": {
		"NODE_ENV": "production"
	},
	"builds": [
		{
			"src": "dist/index.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "dist/index.js"
		}
	],
	"rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
